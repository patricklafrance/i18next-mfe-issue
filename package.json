{
  "name": "react-i18next-mfe-issue",
  "version": "1.0.0",
  "description": "Showcase an issue with react-i18next and MFE.",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "serve-build": "pnpm -r -parallel serve-build",
    "reset": "pnpm run \"/^reset:.*/\"",
    "reset:node-modules": "pnpm -r --include-workspace-root --parallel exec pnpm dlx rimraf node_modules",
    "reset:lock-file": "pnpm dlx rimraf pnpm-lock.yaml"
  },
  "keywords": [],
  "license": "ISC"
}
